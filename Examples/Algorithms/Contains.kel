# A function that checks if list contains a
def contains(list, a) {
    for (e: list) {
        if (e == a) {
            return true
        }
    };
    return false
}

assert contains({1, 2, 3}, 2)
assert !contains({1, 2, 3}, x)
